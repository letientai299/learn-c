- `atexit` is similar to `defer` in Go. And this is new to me.
- `setjump` and `longjump` can be used to jump between functions, while `goto`
  is only usable within a function. I see myself will never use `longjump`, though.
